---
title: 递归 (Recursion)
created: 2026-04-08
updated: 2026-04-08
tags:
  - concept
  - haskell
  - fundamental
sources: 4
related:
  - "[[wiki/sources/cis194-hw1]]"
  - "[[wiki/sources/cis194-hw2]]"
  - "[[wiki/sources/cis194-lecture3-rec-poly]]"
  - "[[Clippings/01-intro]]"
---

# 递归 (Recursion)

## 定义

递归是函数式编程的核心支柱。在 Haskell 中，递归意味着函数调用自身来解决问题。

## 基本结构

每个递归函数都需要两个部分：

1. **基准情况 (Base Case)**: 最简单的情况，直接返回结果
2. **递归情况 (Recursive Case)**: 将问题分解为更小的子问题

## Haskell 示例

### 阶乘

```haskell
factorial :: Integer -> Integer
factorial 0 = 1                    -- 基准情况
factorial n = n * factorial (n-1)  -- 递归情况
```

### 列表长度

```haskell
length' :: [a] -> Int
length' [] = 0          -- 空列表：基准情况
length' (x:xs) = 1 + length' xs  -- 递归情况
```

### 汉诺塔

```haskell
hanoi :: Integer -> Peg -> Peg -> Peg -> [Move]
hanoi 0 _ _ _ = []                                    -- 0个盘子：无需移动
hanoi n a b c = 
    hanoi (n-1) a c b ++ [(a,b)] ++ hanoi (n-1) c b a
```

## 递归与循环的对比

| 递归 | 循环 |
|------|------|
| 声明式 | 命令式 |
| 自然处理树结构 | 自然处理线性结构 |
| 无需可变状态 | 需要计数器 |

## 三大递归模式（Wholemeal Programming）

来自 [[wiki/sources/cis194-lecture3-rec-poly]]：经验丰富的 Haskell 程序员几乎不手写递归，而是用标准库抽象的三大模式：

| 模式 | 作用 | 标准库函数 |
|------|------|-----------|
| **Map** | 对每个元素执行操作 | `map :: (a -> b) -> [a] -> [b]` |
| **Filter** | 按条件保留元素 | `filter :: (a -> Bool) -> [a] -> [a]` |
| **Fold** | 将列表折叠为单个值 | `foldr :: (a -> b -> b) -> b -> [a] -> b` |

```haskell
-- Map：每个元素 +1
map (+1) [1, 2, 3]  -- [2, 3, 4]

-- Filter：只保留偶数
filter even [1, 2, 3, 4]  -- [2, 4]

-- Fold：求和
foldr (+) 0 [1, 2, 3, 4]  -- 10
```

> **核心理念**：用高阶函数思考问题，而不是逐个写递归。类型系统会自动处理递归细节。

## 递归数据结构的三大操作

来自 [[wiki/sources/cis194-hw2]] 的练习展示了处理递归数据结构的通用模式：

```haskell
data MessageTree = Leaf | Node MessageTree LogMessage MessageTree

-- 1. 插入（insert）：递归找到正确位置
insert :: LogMessage -> MessageTree -> MessageTree

-- 2. 构建（build）：从列表依次插入
build :: [LogMessage] -> MessageTree
build = foldr insert Leaf

-- 3. 遍历（inOrder）：中序遍历得到有序列表
inOrder :: MessageTree -> [LogMessage]
inOrder Leaf = []
inOrder (Node left msg right) = inOrder left ++ [msg] ++ inOrder right
```

> **核心思想**：这三种操作（插入、构建、遍历）构成了处理所有递归数据结构的万能范式。

---

## 来自 [[wiki/sources/cis194-hw1]] 的练习

作业要求实现：
- `toDigits`: 递归提取数字
- `sumDigits`: 递归求和
- `hanoi`: 递归解决汉诺塔

---

## 相关概念

- [[wiki/concepts/函数式编程]] - 递归是 FP 的核心
- [[wiki/concepts/标准库函数]] - Map、Filter、Fold 等标准库函数
- [[wiki/concepts/模式匹配]] - 与递归配合使用
- [[wiki/concepts/代数数据类型]] - 递归 ADT 是递归概念的延伸
- [[wiki/sources/cis194-hw2]] - MessageTree 递归数据结构的完整练习
